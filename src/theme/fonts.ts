import { Cairo_400Regular, Cairo_600SemiBold, Cairo_700Bold } from '@expo-google-fonts/cairo';
import { Tajawal_400Regular, Tajawal_500Medium, Tajawal_700Bold } from '@expo-google-fonts/tajawal';

/**
 * Font asset map passed to `useFonts()` in the root layout. Keys MUST match the
 * `fontFamily` token strings in `typography.ts`.
 */
export const fontAssets = {
  Tajawal_400Regular,
  Tajawal_500Medium,
  Tajawal_700Bold,
  Cairo_400Regular,
  Cairo_600SemiBold,
  Cairo_700Bold,
} as const;
