import { Stack } from 'expo-router';

/** Route group: /(app)/support/* — Consultations & Inquiries (Phase 13). */
export default function SupportLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
