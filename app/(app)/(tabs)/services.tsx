/**
 * Leftmost bottom tab (grid icon). Mode-aware: the "كل الأقسام" category grid
 * in Pet Owner mode, the existing Services hub in Veterinarian mode. See
 * `CategoriesTabScreen`. The route name stays `services` so the tab structure,
 * icons and deep links are unchanged.
 */
export { default } from '@/features/hub/screens/CategoriesTabScreen';
