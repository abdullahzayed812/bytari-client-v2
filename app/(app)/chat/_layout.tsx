import { Stack } from 'expo-router';

/** Route group: /(app)/chat/* — Pet Owner ↔ Clinic / Farm Owner ↔ member chat. */
export default function ChatLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
