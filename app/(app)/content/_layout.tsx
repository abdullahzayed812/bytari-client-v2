import { Stack } from 'expo-router';

/** Route group: /(app)/content/* — the Content & Knowledge reading experience (Phase 11). */
export default function ContentLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
