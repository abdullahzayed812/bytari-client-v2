import { Stack } from 'expo-router';

/** Route group: /(app)/tips/* — "أفضل النصائح" (structured care advice from the content module). */
export default function TipsLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
