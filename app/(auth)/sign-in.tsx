/**
 * Route: /(auth)/sign-in — the login screen. Implementation lives in the auth
 * feature so it stays isolated from routing (Phase 2 §3).
 */
export { default } from '@/features/auth/screens/LoginScreen';
